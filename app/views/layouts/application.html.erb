<!DOCTYPE html>
<html>
  <head>
    <title>Farmacia ZI</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag %>
  </head>

  <body class='<%= controller.controller_name %>'>
    <div id="banner">
      <%= link_to image_tag('logoPharma.png', size: "130x65", alt: 'Farmacia Zi'), inicio_path %>
      <span class="title"><%= @page_title %></span>
    </div>
    <div id="columns">
      <div id="side">
        <div id="cart">
          <% if @cart %>
            <%= hidden_div_if(@cart.line_items.empty?, id: 'cart') do %>
              <%= render @cart %>
            <% end %>
          <% end %>
        </div>

        <ul>
          <li><%= link_to 'Tienda', store_path %></li>
          <li><%= link_to 'Contacto', contacto_path %></li>
        </ul>
        <section class="bmi-detalle">
          <h5>Conozca su IMC</h5>
          <p>El IMC proporciona la medida más útil del sobrepeso y la obesidad en la población, pues es la misma para ambos sexos y para los adultos de todas las edades. Sin embargo, hay que considerarla como un valor aproximado porque puede no corresponderse con el mismo nivel de grosor en diferentes personas.</p>
        </section>
        <%= image_tag "imc.png", id: "calculator-img" %>
        <section class="bmi-form">
          <%= render 'body_mass_index/bmi_form' %>
        </section>
        <section id="bmi-result"> </section>
        <section id="bmi-bmi"> </section>
        <div id="bmi-leyend">
          <%= image_tag "IMCcuadro.jpg", size: "200x130" %>
        </div>


        <% if session[:user_id] %>
          <p><%= User.find(session[:user_id]).email %></p>
          <ul>
            <li><%= link_to 'Ordenes',    orders_path   %></li>
            <li><%= link_to 'Productos',  products_path %></li>
            <li><%= link_to 'Usuarios',   users_path    %></li>
            <li><%= link_to 'Contenido',   posts_path    %></li>
            <li><%= link_to 'Catálogo',   catalogos_path    %></li>
          </ul>
          <%= button_to 'Terminar sesión', logout_path, method: :delete %>
        <% else %>
          <section>
            <%= link_to image_tag("TardisIcon.png", alt: ''), login_path, id: "tardis" %>
          </section>
        <% end %>

      </div>
      <div id="main">
        <%= yield %>
      </div>
    </div>
    <%= debug(params) if Rails.env.development? %>
  </body>
</html>
